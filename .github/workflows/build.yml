name: Build image and push

on:
  push:
    branches: [ "dev" ]
  pull_request:
    branches: [ "dev" ]

jobs:

  build:

    runs-on: ubuntu-latest
    permissions: read-all

    steps:
    - run: |
        sudo chmod -R ugo+rwX .
        
    - uses: actions/checkout@v3

    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag aquasec/codesec-scanner:latest

    - name: Login to Aqua registry and pull scanner
      run: |
        echo "Logging into Aqua registry"
        docker login registry.aquasec.com -u denisa.cretu@eviden.com -p ParolaAQUADenissaC.1
        echo "Pulling scanner image"
        docker pull registry.aquasec.com/aquasec/codesec-scanner:latest

    - name: Run Docker
      run: |
        echo "Running Aqua Scanner"
        docker run -v /var/run/docker.sock:/var/run/docker.sock registry.aquasec.com/aquasec/codesec-scanner:latest scan -H https://c3b46c6941-d.cloud.aquasec.com -A 440d5522fee4670c6c617adff47d711b7c8f057e --local aquasec/codesec-scanner:latest
        
    - name: Tag image with Registry
      run: docker tag aquasec/codesec-scanner:latest registry.aquasec.com/aquasec/codesec-scanner:latest

    - name: Push to Registry #something
      run: echo "docker push registry.aquasec.com/aquasec/codesec-scanner:latest
